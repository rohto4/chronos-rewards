# 100%カバレッジ達成のための調査結果

## 調査日
2026-02-23

## 主要な発見

### 1. Vitestカバレッジ設定のベストプラクティス
- v8プロバイダー推奨（高速・正確）
- `exclude`パターンの明示的設定が必須
- 段階的な閾値設定（90% → 95% → 100%）
- ファイル別に100%強制可能: `thresholds: { 'lib/utils/**/*.ts': { 100: true } }`

### 2. Next.js固有の課題
- `next/navigation`は完全モック必須
- `next/image`はReact要素として代替
- Server Componentsは現状Vitestで非対応（E2Eで補完）
- layoutファイルはテスト困難（除外推奨）

### 3. モック戦略
- 外部依存のみモック、ビジネスロジックは実装テスト
- vitest.setup.tsでグローバルモック一括管理
- Supabaseクライアントは完全モック化
- Framer Motionはプレーンな要素に置換

### 4. 段階的アプローチ
- フェーズ1: 純粋関数（低リスク）
- フェーズ2: ストア・UIコンポーネント
- フェーズ3: 統合コンポーネント
- フェーズ4: ページ・アニメーション（高リスク）

### 5. カバレッジエラー対策
- `.tmp`ディレクトリ問題: `rm -rf coverage`で解決
- `coverage.all: true`で動的importも計測
- `reporter: ['text', 'json-summary']`で軽量化

## 成功パターン（実例）

### Supabaseプロジェクトの例
```typescript
vi.mock('next/navigation', () => require('next-router-mock'))
```
- next-router-mockライブラリ使用で完全な動作再現

### Difyプロジェクトの例
```typescript
vi.mock('next/navigation', () => ({
  useRouter: () => ({ push: mockPush }),
  usePathname: () => '/datasets/test-id/documents',
  useSearchParams: () => mockSearchParams,
}))
```
- 個別モック関数で細かい制御

## 避けるべきパターン

1. **過度なコンポーネントモック** → 実装をテストすべき
2. **100%を最初から目指す** → 段階的に進める
3. **除外設定の不足** → 不要ファイルでカバレッジ低下
4. **型定義ファイルの計測** → 無意味なカバレッジ

## 推奨ツール

- Vitest v4.0.18（最新安定版）
- @vitest/coverage-v8（v8プロバイダー）
- @testing-library/react（コンポーネントテスト）
- @testing-library/user-event（ユーザー操作）
- happy-dom or jsdom（DOM環境）

## 次のアクション

1. vitest.config.ts更新（除外・閾値設定）
2. カバレッジエラー修正
3. 純粋関数テスト追加（date-utils, export-utils）
4. ストアテスト完成（genre-store, toast-store）
5. UIコンポーネントテスト（Button, Input, Card）

## 2026-02-23（Supabase client helper）
- `@supabase/auth-helpers-nextjs` は `createClientComponentClient` をエクスポートしなくなっており、`createBrowserClient` / `createServerClient` に移行
- `lib/supabase/client.ts` 側を `createBrowserClient` を使うように直し、ブラウザ用の url/anon-key を環境変数から取得する仕組みに変更

## 2026-02-23（Supabase server helper）
- `createServerComponentClient` も同様に非推奨で `createServerClient` へ移行されたため `lib/supabase/server.ts` 側を置き換え
- Next.js の `cookies()` を `getAll`/`setAll` へブリッジして Supabase に渡すことでセッションの読み書きを維持しつつ型チェック/テストに通るようにした

## 2026-02-23（Genre store typing）
- `lib/stores/genre-store.ts` では Supabase の型推論が `never` を返すため、ローカルで `PostgrestQueryBuilder` を明示的に型補完し、Insert/Update で必要なフィールド型（Record<string, unknown> を含む）を満たす補助型を定義
- 行ったのは: 1) 特定のクエリビルダーを `PostgrestQueryBuilder<{PostgrestVersion:'12'}, GenreSchema, ...>` にキャスト 2) `genreData` に `Record<string, unknown>` 合致させた変数を渡す 3) `task_genres` の戻り値を `TaskGenre` へ明示的にキャスト

## 2026-02-23（Supabase client helper）
- `@supabase/auth-helpers-nextjs` が `createBrowserClient` をエクスポートしなくなっているので、App Router 向けの `createPagesBrowserClient` エイリアスを使ってクライアントインスタンスを再作成
- `createClientComponentClient` は単一オブジェクト引数で URL/キーを受け取るため、`createClientComponentClient<Database>({ supabaseUrl, supabaseKey })` のように呼び出す必要がある。

## 2026-02-23（Supabase server helper）
- `createServerClient` は存在せず、代わりに `createServerComponentClient` を使用。Next.js の `cookies()` を context に渡すだけでトークン取得/更新が行えるため、既存の CookieMethodsServer ブリッジは不要になった。

## 2026-02-23（Task store typing fix）
- `lib/stores/task-store.ts` の Supabase `tasks` クエリでは `genre` / `checklist` を返すデータが `Task` 型として扱われるため `@ts-nocheck` を使っていたが、`TaskWithGenre` へ正規化する小さなヘルパーで型安全に扱えるようにした
- `normalizeTaskRow` で `'genre'` を `null` へフォールバック、`checklist` を空配列へ揃え、`fetchTasks`/`createTask`/`updateTask`/`completeTask` の `single()` 結果には `null` ガードを挟むことで Supabase 型推論のミスマッチを解消
## 2026-02-23�iTask store Supabase�^�S���̍ŏ��C���j
-  �ł͌��JAPI�̓��͌^��  �̂܂܈ێ����ASupabase�N�G���r���_�[���E�ł̂�  �֌^�A�T�[�V��������ƁA�Ăяo�����̌^�j�������� insert �^�S���������ł���B
- �����[�V�����t��  ��  /  ��  �Ƃ��Ď󂯁A ��  �̌^�K�[�h��  �ɐ��K������Ƌ�����ς����Ɉ��S�ɋ��`���ł���B
-  �X�V�͐�p  �����  �^�œn���ƁA �̌^�s��v������ł���B
- ���̏C����A
> chronos-rewards@1.0.0 build
> next build

  �� Next.js 14.2.35
  - Environments: .env.local

   Creating an optimized production build ...
 ? Compiled successfully
   Linting and checking validity of types ... ��  �R���G���[�������������A���i�� �i �̈������s��v�j�Œ�~����B
> chronos-rewards@1.0.0 test:run
> vitest run


[1m[46m RUN [49m[22m [36mv4.0.18 [39m[90mG:/devwork/chronos-rewards[39m

 [32m?[39m lib/stores/user-store.test.ts [2m([22m[2m15 tests[22m[2m)[22m[32m 7[2mms[22m[39m
 [32m?[39m lib/stores/task-store.test.ts [2m([22m[2m9 tests[22m[2m)[22m[32m 9[2mms[22m[39m
 [32m?[39m components/tasks/TaskDetailModal.test.tsx [2m([22m[2m15 tests[22m[2m)[22m[32m 13[2mms[22m[39m
 [32m?[39m lib/utils/reward-utils.test.ts [2m([22m[2m34 tests[22m[2m)[22m[32m 10[2mms[22m[39m

[2m Test Files [22m [1m[32m4 passed[39m[22m[90m (4)[39m
[2m      Tests [22m [1m[32m73 passed[39m[22m[90m (73)[39m
[2m   Start at [22m 15:15:52
[2m   Duration [22m 2.33s[2m (transform 394ms, setup 1.40s, import 306ms, tests 39ms, environment 4.93s)[22m �͈��������S�������B

## 2026-02-23（Task store 型拘束対応 追記）
- task-store では createTask の引数公開型を Database public tasks Insert のまま維持し、クエリ実行境界で tasks テーブル Insert 型へ明示アサーションして整合を取ると、呼び出し側の型破壊を避けられる。
- select の関連データは unknown で受けて型ガードで genre/checklist を狭義化すると、挙動を変えずに型安全を維持できる。
- task_checklist 更新は専用クエリビルダー型を経由することで update 引数の型不一致を解消できる。
- 現時点の build 失敗は lib/supabase/client.ts の createClientComponentClient 呼び出し引数（2引数）に起因し、task-store 由来のエラーは解消済み。

## 2026-02-23（PWA-001）
- `next-pwa` を production でのみ有効化し、`public/manifest.json`＋アイコンを追加して最小の PWA 基盤を整備。
- `app/layout.tsx` で `<link rel="manifest">` と theme-color を定義し、manifest が参照されるようにした。
- `npm run build` は `next-pwa` の service worker 出力を含めて成功（metadata themeColor 警告は既存のページに起因）。
- `npm run test:run` はスクリプト群が実機 Supabase 認証を要求するため `test-genres`/`test-tasks` で未認証・`process.exit(1)` エラーが出て失敗。環境上の制約として認証前提テストは再現できず。

## 2026-02-23（モバイルレスポンシブ対応 - MOBILE-001）
### 修正箇所と理由

#### 1. TaskCard コンポーネント（タッチターゲット最小サイズ 48px 確保）
- **完了ボタン**: `py-2` → `py-3` + `min-h-[48px]` 追加
  - 理由: タッチターゲットが 40px 程度で 48px 未満だったため、最小高さを保証
- **削除ボタン**: `p-2` → `p-3` + `min-w-[48px] min-h-[48px]` 追加
  - 理由: アイコンのみのボタンで小さすぎたため、幅と高さの両方を保証

#### 2. Statistics ページ（375px～414px での表示最適化）
- **ヘッダー padding**: `px-6` → `px-4`
  - 理由: モバイル幅で余白が大きすぎて窮屈に見えたため統一
- **ヘッダーレイアウト**: `flex` → `flex-col sm:flex-row` + `gap-3`
  - 理由: タイトルとエクスポートボタンを小画面では縦並びにして見やすく
- **タイトルサイズ**: `text-2xl` → `text-xl sm:text-2xl`
  - 理由: モバイルで大きすぎてバランスが悪かったため
- **エクスポートボタン**: `flex` → `hidden md:flex`
  - 理由: 小画面ではスペースが限られているため非表示にして UI をシンプルに
- **メインコンテンツ padding**: `px-6` → `px-4`
  - 理由: ヘッダーと統一して一貫性を保つ
- **期間選択**: `flex gap-2` → `flex flex-wrap gap-2`
  - 理由: 3つのボタンが横並びで 375px では窮屈だったため折り返し対応

#### 3. Calendar ページ（余白の統一）
- **ヘッダー padding**: `px-6` → `px-4`
- **メインコンテンツ padding**: `px-6` → `px-4`
  - 理由: Statistics ページと統一し、モバイルでの余白を適切に

### 検証結果
- ビルド: ✅ 成功（型チェック・Lint ともにエラーなし）
- テスト: ✅ 主要テスト全て通過（111 tests passed）
- LSP 診断: ✅ 変更ファイル全てエラーなし

### モバイルレスポンシブのベストプラクティス
1. **タッチターゲットサイズ**: 最小 48px × 48px を `min-w-[48px] min-h-[48px]` で保証
2. **padding 統一**: モバイルでは `px-4`、デスクトップでは必要に応じて `px-6` 以上
3. **フレックスボックス折り返し**: `flex-wrap` を使って小画面対応
4. **レスポンシブユーティリティ**: `hidden md:flex`, `flex-col sm:flex-row` などで段階的に表示を切り替え
5. **文字サイズ調整**: `text-xl sm:text-2xl` のようにブレークポイント別にサイズ指定

### 今後の注意点
- 新規コンポーネント作成時は最初から `min-h-[48px]` を含めたタッチターゲットサイズを確保
- padding/margin は `px-4` を基準にし、デスクトップで必要な場合のみ大きくする
- ボタンやリンクは視覚的に小さく見えても、タッチエリアは十分に確保する

## 2026-02-23（COVERAGE-001: TaskCard & ChecklistEditor）
- **カバレッジ対象分岐:** TaskCard のジャンル/説明非表示、完了済み時のアクション排除、チェックリスト進捗不表示。ChecklistEditor の追加ガード・Enter キー処理・非 Enter キー無視、および削除機能。
- **検証手段:** `npm run test:run` / `npm run test:coverage` を通じて各ケースを再確認。
- **残ブロッカー:** なし

## 2026-02-23（Input/Textarea 分岐カバレッジ）
- **Input:** ラベルと必須インジケータ、左/右アイコン、ヘルパーテキスト、エラーメッセージ、disabled+readOnly、カスタム className を組み合わせてクラス/aria 指向の分岐を網羅。
- **Textarea:** ヘルパーテキストと文字数カウント、有効な maxLength 演算、エラー時の helper 非表示、disabled 状態、showCount が maxLength に依存する分岐を実データで検証。entry の `value` + `onChange` で counter が 3/10 になることも確認。
- **コマンド:** `npm run test:run` と `npm run test:coverage` を実行し、Textareas/Input の branch coverage が 100% 近辺（Input 100%、Textarea 94.7%）まで向上。

## 2026-02-23（COVERAGE-001: Textarea 必須インジケータ）
- line 62 の  分岐（必須アスタリスク表示）を  で実挙動検証して通した
- coverage:  は statements/branches/functions/lines 全て 100
## 2026-02-23（COVERAGE-001: Textarea 必須インジケータ）
- line 62 の `props.required` 分岐（必須アスタリスク表示）を `within(label).getByText('*')` で実挙動検証して通した
- coverage: `components/ui/Textarea.tsx` は statements/branches/functions/lines 全て 100%、branch 100% になった
- 残ブロッカー: なし

## 2026-02-23（COVERAGE-001: TaskCard/Button/Badge 分岐補完）
- **TaskCard (line 118)**: Enter/Space の `onKeyDown` を `onClick` あり/なしで分別し、支配された分岐（呼び出し/無効化）を実挙動で通した。これで該当 line が 2 度評価されることを確認。
- **Button (line 123)**: `isLoading` かつ `leftIcon` ありの状態で `rightIcon` 描画を検証し、右アイコン branch が `left/loading` Gate に影響されず通ることを確認。
- **Badge (lines 75/80)**: `dot` + variant `primary`/`purple` の dot 要素を DOM 検索し、`bg-blue-600`/`bg-purple-600` クラスで色分岐を検証。
- **テスト:** `npm run test:run`, `npm run test:coverage` を実行して動作/カバレッジを再確認。
- **最終カバレッジ:** All files statements 99.52%, branches 100%, functions 98.36%, lines 99.5%.
- **残ブロッカー:** なし。

## 2026-02-23（COVERAGE-001: Card コンポーネント網羅）
- **Card.tsx の通過領域:** Card 本体の default variant クラス（`bg-slate-900 border border-slate-800 shadow-xl`）+ `className` 併合、outline/ghost variant の各スタイル、CardHeader/Title/Description/Body/Footer の `className` マージ、CardDescription の `text-sm`/`text-slate-400`/`mt-1.5` branch。
- **テスト:** 新規 `components/ui/Card.test.tsx` でレンダリング → `getByTestId`/`getByText` で各サブコンポーネントを検証。
- **最終カバレッジ:** All files statements/branches/functions/lines 100%、`components/ui/Card.tsx` も 100% (branch 100%)。
- **残ブロッカー:** なし。
